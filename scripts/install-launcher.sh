#!/bin/sh
set -eu

echo "Remote installation is disabled in this security-hardened fork." >&2
echo "Build the pinned source locally and install the verified package from launcher/artifacts." >&2
exit 1
